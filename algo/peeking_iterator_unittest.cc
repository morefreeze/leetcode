#include <gtest/gtest.h>
#include "peeking_iterator.cpp"

class PeekingIteratorTest: public testing::Test{
    protected:
};
TEST_F(PeekingIteratorTest, Small){
    // I don't has the Iterator implement, so I can't test it, just make sure
    // this can be compelied and submit it
}
